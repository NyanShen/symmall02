<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sym.system.mapper.SysRoleMapper">

    <resultMap id="SysRoleResult" type="com.sym.system.domain.SysRole">
        <id property="roleId" column="role_id"/>
        <result property="roleName" column="role_name"/>
        <result property="roleKey" column="role_key"/>
        <result property="roleSort" column="role_sort"/>
        <result property="dataScope" column="data_scope"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>
    <!-- 关联表查询：通过userId查询对应的角色列表 -->
    <select id="selectRolesByUserId" resultMap="SysRoleResult">
        SELECT r.*
        FROM sys_role r
        INNER JOIN sys_user_role ur ON r.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
        AND r.del_flag = '0'
        AND r.status = '0'
    </select>

    <!--
    关联表查询：通过roleId查询对应的菜单列表
     DISTINCT
    去重关键字，用于去除重复的记录
    确保返回的结果集中每个权限标识只出现一次
    -->
    <select id="selectPermsByRoleId" resultType="java.lang.String">
        SELECT DISTINCT m.perms
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.menu_id = rm.menu_id
        WHERE rm.role_id = #{roleId}
        AND m.status = '0'
        AND m.perms IS NOT NULL
        AND m.perms != ''
    </select>
</mapper>